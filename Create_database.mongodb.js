// Creating Database and collection
const database = "GlobalWeather_data";
const collection = "GlobalWeather_data";

// Creating a new database.
use(database);

// Creating a new collection.
db.createCollection(collection);
